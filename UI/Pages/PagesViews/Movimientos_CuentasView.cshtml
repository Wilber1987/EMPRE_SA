@page
@using API.Controllers
@using APPCORE.Security
@{
     if (!SecurityController.Auth(HttpContext.Session.GetString("seassonKey")))
    {
        Response.Redirect("../Security/login");
        return;
    }
    @if (!SecurityController.HavePermission(Permissions.GESTION_MOVIMIENTOS.ToString(), HttpContext.Session.GetString("seassonKey")) )//todo
    {
       Response.Redirect("../Security/login");
        return;
    }    
}
<script src='~/Views/Movimientos_CuentasView.js' type='module'></script>
<div id='MainBody'></div>
