@page
@using API.Controllers
@using CAPA_DATOS.Security
@{
     if (!SecurityController.Auth(HttpContext.Session.GetString("seassonKey")))
    {
        Response.Redirect("../Security/login");
        return;
    }
    @if (!SecurityController.HavePermission(PermissionsEnum.GESTION_EMPEÑOS.ToString(), HttpContext.Session.GetString("seassonKey")) )//todo
    {
       Response.Redirect("../Security/login");
        return;
    }    
}
<script src='~/Views/Transaction_ContratosView.js' type='module'></script>
<div id='MainBody'></div>
